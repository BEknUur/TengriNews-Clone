map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

upstream django_app {
    server web:8000;
}

server {
    listen 80;
    server_name localhost 127.0.0.1;

    client_max_body_size 25M;

    # Proxy all application requests to the Django/Gunicorn backend
    location / {
        proxy_pass http://django_app;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_redirect off;
        proxy_read_timeout 60;
    }

    # Serve static files collected by Django (mounted from named volume)
    location /static/ {
        alias /var/www/static/;
        expires 30d;
        add_header Cache-Control "max-age=2592000";
    }

    # Serve media/uploads (mounted from named volume)
    location /media/ {
        alias /var/www/media/;
    }

    # WebSocket/long-polling endpoint upgrade
    location /ws/ {
        proxy_pass http://django_app;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_read_timeout 3600;
    }
}
